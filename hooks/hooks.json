{
  "description": "Keep the Engineering Partner discipline active in main sessions and subagents.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${PLUGIN_ROOT}/hooks/engineering-partner.js\" inject SessionStart",
            "commandWindows": "if (Get-Command node -ErrorAction SilentlyContinue) { node \"$env:PLUGIN_ROOT\\hooks\\engineering-partner.js\" inject SessionStart }",
            "timeout": 5,
            "statusMessage": "Loading the Engineering Partner discipline.",
            "additionalContextLimit": 750
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${PLUGIN_ROOT}/hooks/engineering-partner.js\" inject SubagentStart",
            "commandWindows": "if (Get-Command node -ErrorAction SilentlyContinue) { node \"$env:PLUGIN_ROOT\\hooks\\engineering-partner.js\" inject SubagentStart }",
            "timeout": 5,
            "statusMessage": "Loading the Engineering Partner discipline.",
            "additionalContextLimit": 750
          }
        ]
      }
    ]
  }
}
