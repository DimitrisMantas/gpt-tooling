diff --git a/.codex-plugin/plugin.json b/.codex-plugin/plugin.json
index 474acfb..5046a4e 100644
--- a/.codex-plugin/plugin.json
+++ b/.codex-plugin/plugin.json
@@ -3 +3 @@
-  "version": "4.9.0",
+  "version": "4.9.0+gpt-tooling.1",
diff --git a/.openclaw/skills/ponytail/SKILL.md b/.openclaw/skills/ponytail/SKILL.md
index a3e4d94..0768f3c 100644
--- a/.openclaw/skills/ponytail/SKILL.md
+++ b/.openclaw/skills/ponytail/SKILL.md
@@ -54,11 +53,0 @@ every sibling caller still broken. Fix it once, where all callers route through.
-## Output
-
-Code first. Then at most three short lines: what was skipped, when to add it.
-No essays, no feature tours, no design notes. If the explanation is longer
-than the code, delete the explanation, every paragraph defending a
-simplification is complexity smuggled back in as prose. Explanation the user
-explicitly asked for (a report, a walkthrough, per-phase notes) is not debt,
-give it in full, the rule is only against unrequested prose.
-
-Pattern: `[code] → skipped: [X], add when [Y].`
-
diff --git a/hooks/ponytail-instructions.js b/hooks/ponytail-instructions.js
index 3ec3980..8851a74 100644
--- a/hooks/ponytail-instructions.js
+++ b/hooks/ponytail-instructions.js
@@ -65,4 +64,0 @@ function getFallbackInstructions(mode) {
-    '## Output\n\n' +
-    'Code first. Then at most three short lines: what was skipped, when to add it. ' +
-    'If the explanation is longer than the code, delete the explanation. ' +
-    'Explanation the user explicitly asked for is not debt, give it in full.\n\n' +
diff --git a/hooks/ponytail-runtime.js b/hooks/ponytail-runtime.js
index db0c477..5bb2e48 100644
--- a/hooks/ponytail-runtime.js
+++ b/hooks/ponytail-runtime.js
@@ -59 +59 @@ function writeHookOutput(event, mode, context = '') {
-    const output = { systemMessage: `PONYTAIL:${mode.toUpperCase()}` };
+    const output = {};
diff --git a/scripts/check-versions.js b/scripts/check-versions.js
index b2b0d6f..855b45c 100644
--- a/scripts/check-versions.js
+++ b/scripts/check-versions.js
@@ -17,0 +18 @@ const PINNED_SEMVER = /^\d+\.\d+\.\d+$/;
+const CODEX_BUILD = '+gpt-tooling.1';
@@ -45 +46,4 @@ const versions = VERSION_FILES.map((relPath) => {
-  if (typeof version !== 'string' || !PINNED_SEMVER.test(version)) {
+  const base = relPath === '.codex-plugin/plugin.json' && typeof version === 'string' && version.endsWith(CODEX_BUILD)
+    ? version.slice(0, -CODEX_BUILD.length)
+    : version;
+  if (typeof version !== 'string' || !PINNED_SEMVER.test(base)) {
@@ -49 +53 @@ const versions = VERSION_FILES.map((relPath) => {
-  return [relPath, version];
+  return [relPath, version, base];
@@ -53 +57 @@ const versions = VERSION_FILES.map((relPath) => {
-const distinct = [...new Set(versions.map(([, v]) => v))];
+const distinct = [...new Set(versions.map(([, , base]) => base))];
diff --git a/skills/ponytail/SKILL.md b/skills/ponytail/SKILL.md
index 02c0712..d338a6c 100644
--- a/skills/ponytail/SKILL.md
+++ b/skills/ponytail/SKILL.md
@@ -66,11 +65,0 @@ every sibling caller still broken. Fix it once, where all callers route through.
-## Output
-
-Code first. Then at most three short lines: what was skipped, when to add it.
-No essays, no feature tours, no design notes. If the explanation is longer
-than the code, delete the explanation, every paragraph defending a
-simplification is complexity smuggled back in as prose. Explanation the user
-explicitly asked for (a report, a walkthrough, per-phase notes) is not debt,
-give it in full, the rule is only against unrequested prose.
-
-Pattern: `[code] → skipped: [X], add when [Y].`
-
diff --git a/tests/gemini-extension.test.js b/tests/gemini-extension.test.js
index db09661..2eca74a 100644
--- a/tests/gemini-extension.test.js
+++ b/tests/gemini-extension.test.js
@@ -18,0 +19 @@ const PINNED_SEMVER = /^\d+\.\d+\.\d+$/;
+const CODEX_BUILD = '+gpt-tooling.1';
@@ -61,2 +62,5 @@ test('version stays aligned with the other plugin manifests', () => {
-    assert.match(manifest.version, PINNED_SEMVER, `${rel} version must be pinned semver`);
-    return manifest.version;
+    const version = rel === '.codex-plugin/plugin.json' && typeof manifest.version === 'string' && manifest.version.endsWith(CODEX_BUILD)
+      ? manifest.version.slice(0, -CODEX_BUILD.length)
+      : manifest.version;
+    assert.match(version, PINNED_SEMVER, `${rel} version must be pinned semver`);
+    return version;
diff --git a/tests/hooks.test.js b/tests/hooks.test.js
index 44ee922..b57b4df 100644
--- a/tests/hooks.test.js
+++ b/tests/hooks.test.js
@@ -62 +62 @@ let output = JSON.parse(result.stdout);
-assert.equal(output.systemMessage, 'PONYTAIL:ULTRA');
+assert.equal(output.systemMessage, undefined);
@@ -78 +78 @@ output = JSON.parse(result.stdout);
-assert.equal(output.systemMessage, 'PONYTAIL:LITE');
+assert.equal(output.systemMessage, undefined);
@@ -104 +104 @@ output = JSON.parse(result.stdout);
-assert.equal(output.systemMessage, 'PONYTAIL:OFF');
+assert.equal(output.systemMessage, undefined);
@@ -283 +283 @@ output = JSON.parse(result.stdout);
-assert.equal(output.systemMessage, 'PONYTAIL:FULL');
+assert.equal(output.systemMessage, undefined);
